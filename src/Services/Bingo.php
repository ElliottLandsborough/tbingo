<?php

namespace ElliottLandsborough\TerminalBingo\Services;

use Exception;

class Bingo
{
    /*
        private function _stdin_stream()
        {
            while ($line = fgets(STDIN)) {
                yield $line;
            }
        }

        foreach (stdin_stream() as $line) {
            // do something with the contents coming in from STDIN
        }
    */
}
